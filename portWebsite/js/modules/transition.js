// ==========================================================================
// 页面间过渡模块
// —— 过渡 A（首页→进入网页）/ 过渡 B（进入网页→进入作品）
// —— 返回倒放（过渡 B 反向）/ 下一个卷轴（按 order 递增）
// —— 缓动：关键帧查表 + smoothstep 插值（复用 frin-site 方案）
// —— 运动方向语法：前进 = 向下沉没消失；返回 = 向上升起出现
// —— 检索标签：可调整-速度（各过渡时长/缓动）—— 参数待用户提供
// ==========================================================================

// ------------------------------------------------------------------
// 过渡参数（待实现时填入）—— 可调整-速度
// ------------------------------------------------------------------
// export const TRANSITION = {
//   durationA: 1500,  // 可调整-速度：过渡 A 总时长（ms）
//   durationB: 800,   // 可调整-速度：过渡 B 总时长（ms）
//   scrollDur: 700,   // 可调整-速度：下一个卷轴时长（ms）
// };

// —— smoothstep 缓动（占位）——
// —— 过渡 A：首页 → 进入网页（头像作锚点，交叉重叠）——
// —— 过渡 B：进入网页 → 进入作品（被点缩略图作锚点，头像随整页淡出）——
// —— 返回：倒放过渡 B，主 mp4 缩回缩略图飞回原网格槽位 ——
// —— 下一个：纵向卷轴过渡，视口内才播放新作品 mp4 ——

export {};
