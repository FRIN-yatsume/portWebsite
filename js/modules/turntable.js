// ==========================================================================
// 模型旋转帧序列 turntable 模块（进入作品右下橙框）
// —— 默认闲置自动慢转
// —— 用户拖拽接管手动旋转（pointermove 控制当前帧）
// —— 检索标签：可调整-速度（自转速度/拖拽灵敏度）—— 帧数/每帧角度待用户提供
// ==========================================================================

// ------------------------------------------------------------------
// turntable 参数（待实现时填入）
// ------------------------------------------------------------------
// export const TURNTABLE = {
//   idleFps: 12,        // 可调整-速度：闲置自转帧率
//   dragSensitivity: 1, // 可调整-速度：拖拽旋转灵敏度（每像素多少帧）
// };

// —— 帧序列 + 闲置自转 + 拖拽接管（待实现）——

export {};
